import classes from './Navigation.module.css'
import NavLinks from './NavLinks'


const DeskBar = () => {
  

  return (
    <nav className={classes.deskBar}>
      <NavLinks />
    </nav>
    
    
    
  );
};

export default DeskBar;